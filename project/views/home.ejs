<!DOCTYPE html>
<html lang="kr">
  <head>
    <meta charset="UTF-8" />
    <title>After Reading</title>
    <link rel="shortcut icon" href="note.png" type="image/x-icon" />
    <link rel="stylesheet" href="home.css" />
  </head>
  <body>
    <div class="homeHead">
      <div><a href="/logout">로그아웃</a></div>
      <div><a name="userName" href="/logout"><%= userName %></a></div>
    </div>
    <div class="searchDiv">
      <form class="search" action="/booksearch">
        <input
          type="text"
          name="book"
          placeholder="책 제목"
          style="width: 40%;"
          required
        />
        <input
          type="text"
          name="author"
          placeholder="작가 이름"
          style="width: 10%;"
          required
        />
        <input type="submit" value="검색" />
      </form>
    </div>
    <div class="result">
      <% if (typeof(results) === 'string' ) { %>
      <p>검색할 단어를 확인 후 다시 검색해주세요.</p>
      <% } else if (results !== undefined ){%>
      <table class="resultsTable">
        <% for (let i = 0; i < repeat; i++) { %>
        <tr>
          <td><img class="resultImg" src=" <%= results[i].image %> " /></td>
          <td><%- results[i].title %></td>
          <td><%- results[i].author %></td>
          <td>
            <form><input type="submit" value="작성하기" /></form>
          </td>
        </tr>
        <% } %>
      </table>
      <% } %>
    </div>
  </body>
</html>
