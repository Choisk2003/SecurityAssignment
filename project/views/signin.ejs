<!DOCTYPE html>
<html lang="kr">
  <head>
    <meta charset="UTF-8" />
    <title>After Reading</title>
    <link rel="shortcut icon" href="note.png" type="image/x-icon" />
    <link rel="stylesheet" href="/signin.css" />
  </head>
  <body>
    <div class="signinup">
      <div class="signin">
        <h1 class="signTitle">독후감</h1>
        <br />
        <div class="signinForm">
          <form class="signinForm" action="/sign" method="POST">
            <input
              class="inputform"
              name="name"
              type="text"
              placeholder="이메일 또는 아이디"
              required
            />
            <input
              class="inputform"
              name="password"
              type="password"
              placeholder="비밀번호"
              required
            />
            <input
              class="inputform"
              type="submit"
              value="로그인"
              style="margin-left: 45px;"
            />
          </form>
          <% if(message) {%>
          <a style="font-size: 10px; color: red;"><%= message %></a>
          <%}%>
        </div>
      </div>
      <div class="signup">
        계정이 없으신가요? <a href="/sign/up">회원가입</a>
      </div>
    </div>
  </body>
</html>
